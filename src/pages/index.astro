---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('blog');
const latestPost = posts[0]; // Assuming posts are sorted by date
---

<Layout title="Software & KI Blog" description="Ein Blog über Software-Entwicklung, KI und die Zukunft des Entwickelns">
  <section style="margin-bottom: 4rem;">
    <h1>Willkommen!</h1>
    <p>
      Hier schreibe ich über Software-Entwicklung, KI und wie sich unsere Arbeit als Entwickler verändert.
      Meine Perspektive ist die eines erfahrenen Software-Entwicklers, der die Veränderungen in der Branche
      aktiv mitgestaltet und reflektiert.
    </p>
  </section>

  {latestPost && (
    <section>
      <h2>Neuester Artikel</h2>
      <article style="margin-top: 1rem;">
        <h3 style="margin: 0;">
          <a href={`/blog/${latestPost.slug}`} style="color: #333; text-decoration: none;">
            {latestPost.data.title}
          </a>
        </h3>
        <time datetime={latestPost.data.pubDate.toISOString()} style="color: #666; font-size: 0.9rem;">
          {latestPost.data.pubDate.toLocaleDateString('de-DE')}
        </time>
        <p>{latestPost.data.description}</p>
        <a href="/blog" style="display: inline-block; margin-top: 1rem;">Alle Artikel →</a>
      </article>
    </section>
  )}
</Layout> 